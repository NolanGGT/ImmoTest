# Production environment for Next.js
# NEXT_PUBLIC_* vars are baked into the build — set them as Docker build args in Coolify

NEXT_PUBLIC_API_URL=https://api.immosafe.fr
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_live_...
NEXT_PUBLIC_MAPBOX_TOKEN=pk.eyJ1...
